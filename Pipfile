[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
authlib = "*"
requests = "*"

[dev-packages]
ipdb = "*"
bandit = "*"
flake8 = "*"
black = "*"

[requires]
python_version = "3.10"

[scripts]
tests = "python -m unittest discover -s 'test'"
# stop the build if there are Python syntax errors or undefined names
syntax = "flake8 ponos --count --select=E9,F63,F7,F82 --show-source --statistics"
# exit-zero treats all errors as warnings. The GitHub editor is 127 chars wide
format = "flake8 ponos --count --exit-zero --max-complexity=10 --max-line-length=127 --statistics"