[tool.poetry]
name = "hyacinth"
version = "0.1.0"
description = ""
authors = ["Anson MacKeracher <anson@legalmate.co>"]
license = "MIT"
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
Authlib = "^1.2.0"
requests = "^2.28.2"

[tool.poetry.group.dev.dependencies]
ipdb = "^0.13.11"
bandit = "^1.7.4"
flake8 = "^6.0.0"
black = "^22.12.0"
pytest = "^7.2.1"

[tool.poetry.scripts]
test = "test:run"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
